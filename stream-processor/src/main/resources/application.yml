spring:
  cloud:
    function:
      scan:
        packages: com.codependent.serverless.processor 
    stream:
      bindings:
        input:
          destination: raw-events
        output: 
          destination: processed-events

server:
  port: 8181
